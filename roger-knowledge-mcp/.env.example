MCP_HOST=127.0.0.1
MCP_PORT=8765
MCP_PATH=/mcp
PUBLIC_HOSTNAME=mcp.fleiglabs.cc
ORIGIN_HOSTNAME=origin-mcp.fleiglabs.cc

# Set true only when origin-side Cloudflare Access JWT validation is required.
REQUIRE_CLOUDFLARE_ACCESS_JWT=false
